
plugin.tx_news {

	view {
		// Clear the old settings because otherwise the overlay mechanism
		// will not work
		templateRootPath >

		// Re-initialize the news paths for the overlay mechanism
		partialRootPaths.news = {$plugin.tx_news.view.partialRootPath}

		// add our own directory to the overlay configuration
		partialRootPaths.news_richteaser = EXT:news_richteaser/Resources/Private/Partials/
	}

	persistence {

		classes {
			Tx_News_Domain_Model_News {
				subclasses {
					0 = Int\NewsRichteaser\Domain\Model\NewsRichteaser
				}
			}

			Int\NewsRichteaser\Domain\Model\NewsRichteaser {
				mapping {
					recordType = 0
					tableName = tx_news_domain_model_news
				}
			}
		}
	}
}

lib.tx_news.contentElementRenderingTeaser < lib.tx_news.contentElementRendering